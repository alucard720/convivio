CREATE TABLE convivio (
id INT NOT NULL AUTO_INCREMENT,
NOMBRE_TITULAR VARCHAR(100),
ACOPANANTE1 VARCHAR(100),
ACOPANANTE2 VARCHAR(100),
NINO VARCHAR(100),
EDAD INT,
REQUERIMIENRO VARCHAR(100),
PROVINCIA VARCHAR(100),
HOTEL VARCHAR(100),
PRIMARY KEY (id)
);

BULK INSERT convivio
FROM '/Users/miguelsantana/projects/conviviomaetros/convivio_db_1.csv'
WITH
(
    FIRSTROW = 2,
    FIELDTERMINATOR = ',',
    ROWTERMINATOR = '\n',
    TABLOCK

)